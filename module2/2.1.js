"use strict";
{
    // 
    // type assertion
    let anything;
    anything = 222;
    anything.
    ;
    const kgToGm = (value) => {
        if (typeof value === 'string') {
            const conNum = parseFloat(value) * 1000;
            return conNum;
        }
    };
    try {
    }
    catch (error) {
        console.log(error).message;
    }
}
